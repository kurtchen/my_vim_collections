" for different computers {{{
    if filereadable(expand('~/.vimrc.bundles.local.local'))
        source ~/.vimrc.bundles.local.local
    endif
" }}}

" so here's my the plugins {{{
    Bundle 'gerw/vim-latex-suite'
    Bundle 'chikamichi/mediawiki.vim'
    Bundle 'kurtchen/vim-git-log'
    Bundle 'kurtchen/grep.vim'
    Bundle 'guns/xterm-color-table.vim'
" }}}
